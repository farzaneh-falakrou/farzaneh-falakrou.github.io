---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.mjs
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/components/index.ts
  - src/app/about/page.tsx
  - src/app/blog/
  - src/app/gallery/
  - src/app/work/
  - src/app/api/authenticate/
  - src/app/api/check-auth/
  - src/app/api/og/
  - src/app/api/rss/
  - src/components/Mailchimp.tsx
  - src/components/RouteGuard.tsx
  - src/components/ThemeToggle.tsx
  - src/components/ThemeToggle.module.scss
  - src/components/Header.tsx
  - src/components/Header.module.scss
  - src/components/Footer.tsx
  - src/components/FooterCTA.tsx
  - src/components/HeroHeadline.tsx
  - src/components/HomeIntro.tsx
  - src/components/HowIWork.tsx
  - src/components/MarqueeTicker.tsx
  - src/components/ProcessAndTools.tsx
  - src/components/ProjectCard.tsx
  - src/components/ProjectCard.module.scss
  - src/components/Providers.tsx
  - src/components/StatsStrip.tsx
  - src/components/HeadingLink.tsx
  - src/components/HeadingLink.module.scss
  - src/components/mdx.tsx
  - src/components/ScrollToHash.tsx
  - src/components/breakpoints.scss
  - src/components/about/
  - src/components/blog/
  - src/components/gallery/
  - src/components/work/
  - src/resources/
  - src/types/
  - src/utils/formatDate.ts
  - src/utils/utils.ts
  - package-lock.json
autonomous: true
requirements: [INFRA-01]
must_haves:
  truths:
    - "Once UI and unused legacy components, routes, and dependencies are gone from the repo"
    - "`npm install` runs cleanly with no missing peer dependencies"
    - "Build attempts surface only intentional gaps (the now-empty layout.tsx referencing missing components); no Once UI import resolution errors"
  artifacts:
    - path: "package.json"
      provides: "Cleaned dependency manifest"
      contains: "next, react, @next/mdx, gray-matter, classnames — no @once-ui-system/core, no next-mdx-remote, no sass, no react-icons, no react-rough-notation, no cookie, no transliteration, no tzdata"
    - path: "next.config.mjs"
      provides: "Cleaned Next config"
      contains: "withMDX wrapper, pageExtensions; no sassOptions, no transpilePackages for next-mdx-remote, no remotePatterns to www.google.com"
    - path: "src/app/page.tsx"
      provides: "One-line placeholder home"
      contains: "default export Home returning a single main element with link to /styleguide"
    - path: "src/components/index.ts"
      provides: "Reset barrel (empty or one-line placeholder)"
      contains: "no exports of deleted components"
  key_links:
    - from: "src/app/layout.tsx"
      to: "(no Once UI imports remain)"
      via: "After strip, layout.tsx is reduced to a minimal valid layout returning <html><body>{children}</body></html> with no module-resolution failures; the rich layout is rewritten in PLAN 02"
      pattern: "no import from '@once-ui-system'"
---

<objective>
Strip the existing Next.js scaffold down to what serves v1. Remove Once UI, theme system, legacy components, deprecated routes, and unused dependencies. Reduce `layout.tsx` and `page.tsx` to minimal valid placeholders so the project builds cleanly after the strip. Subsequent plans rebuild on top of this clean slate.

Purpose: Honor D-01..D-06 (strip in place) and INFRA-01 before any new code is written. Sketches are the source of truth (user memory); the existing scaffold contributes only structural patterns.

Output: Cleaned `package.json`, cleaned `next.config.mjs`, minimal `src/app/layout.tsx`, placeholder `src/app/page.tsx`, deleted legacy files/routes/folders, regenerated `package-lock.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-SKELETON.md
@package.json
@next.config.mjs
@src/app/layout.tsx
@src/app/page.tsx
@src/components/index.ts

<interfaces>
<!-- Package manager: npm (package-lock.json present). Use `npm install` / `npm run build` / `npm run lint`. -->
<!-- Deletion list is authoritative — see PATTERNS.md "Deletions" table and CONTEXT D-03..D-06. -->
<!-- All `.module.scss` files in this phase are deleted (D-11 drops sass); no conversion to .module.css needed because the components themselves are deleted. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy components, routes, resources, types, and unused utils</name>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (Deletions table, lines 50–86 — authoritative path list)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-03, D-04, D-06)
    - src/components/index.ts (currently exports many of these — reset in Task 3)
  </read_first>
  <action>
    Delete every path enumerated in PATTERNS.md "Deletions" table (per D-03, D-04, D-06). Scope:

    Component files (and their `.module.scss` siblings) under `src/components/`: `Mailchimp.tsx`, `RouteGuard.tsx`, `ThemeToggle.tsx` + `ThemeToggle.module.scss`, `Header.tsx` + `Header.module.scss`, `Footer.tsx`, `FooterCTA.tsx`, `HeroHeadline.tsx`, `HomeIntro.tsx`, `HowIWork.tsx`, `MarqueeTicker.tsx`, `ProcessAndTools.tsx`, `ProjectCard.tsx` + `ProjectCard.module.scss`, `Providers.tsx`, `StatsStrip.tsx`, `HeadingLink.tsx` + `HeadingLink.module.scss`, `mdx.tsx`, `ScrollToHash.tsx`, `breakpoints.scss`.

    Component subdirectories under `src/components/`: `about/`, `blog/`, `gallery/`, `work/` (delete recursively).

    Route files and directories under `src/app/`: `api/authenticate/` (recursive), `api/check-auth/` (recursive), `api/og/` (recursive — default delete per D-04), `api/rss/` (recursive — default delete per D-04), `blog/` (recursive), `gallery/` (recursive), `about/page.tsx`, `work/page.tsx`, `work/[slug]/` (recursive), `work/projects/` (recursive). `src/app/not-found.tsx` stays as-is in this task — Plan 02 Task 5 decides its fate.

    Folders to wipe: `src/resources/` (all files — D-06) and `src/types/` (all files — D-06). Delete the folders themselves; Plan 02 creates `src/styles/` fresh.

    Util files: delete `src/utils/formatDate.ts` and `src/utils/utils.ts` (D-06 / unused in v1). Remove the now-empty `src/utils/` folder.

    Keep untouched: `src/app/favicon.ico`, `src/app/robots.ts`, `src/app/sitemap.ts`, `src/app/layout.tsx` (reduced in Task 2), `src/app/page.tsx` (reduced in Task 2), `next.config.mjs` (rewritten in Task 4), `tsconfig.json`, `eslint.config.mjs`, `biome.json`, `package.json` (rewritten in Task 4), `public/`, `legacy/`.

    Use `git rm -r` for folders to keep version control honest. Do NOT delete `src/components/index.ts` — reset its contents in Task 3.
  </action>
  <verify>
    <automated>cmd /c "if exist src\components\Mailchimp.tsx (exit 1) else (if exist src\components\RouteGuard.tsx (exit 1) else (if exist src\resources (exit 1) else (if exist src\types (exit 1) else (if exist src\app\api\authenticate (exit 1) else (if exist src\app\blog (exit 1) else (if exist src\app\gallery (exit 1) else (exit 0)))))))"</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/components/Mailchimp.tsx` does not exist
    - source: `src/components/RouteGuard.tsx` does not exist
    - source: `src/components/Header.tsx` does not exist
    - source: `src/components/ThemeToggle.module.scss` does not exist
    - source: `src/components/mdx.tsx` does not exist
    - source: `src/components/ScrollToHash.tsx` does not exist
    - source: `src/components/breakpoints.scss` does not exist
    - source: `src/components/about` directory does not exist
    - source: `src/components/blog` directory does not exist
    - source: `src/components/gallery` directory does not exist
    - source: `src/components/work` directory does not exist
    - source: `src/resources` directory does not exist
    - source: `src/types` directory does not exist
    - source: `src/utils` directory does not exist
    - source: `src/app/api/authenticate` directory does not exist
    - source: `src/app/api/check-auth` directory does not exist
    - source: `src/app/api/og` directory does not exist
    - source: `src/app/api/rss` directory does not exist
    - source: `src/app/blog` directory does not exist
    - source: `src/app/gallery` directory does not exist
    - source: `src/app/about/page.tsx` does not exist
    - source: `src/app/work/page.tsx` does not exist
    - source: `src/app/work/[slug]` directory does not exist
    - source: `src/app/favicon.ico` still exists
    - source: `src/app/robots.ts` still exists
    - source: `src/app/sitemap.ts` still exists
  </acceptance_criteria>
  <done>
    All paths in PATTERNS.md "Deletions" table are removed from the working tree and staged for commit. `src/app/layout.tsx`, `src/app/page.tsx`, `src/components/index.ts`, `next.config.mjs`, `package.json` are still present (they are rewritten by later tasks). Files explicitly listed as "keep" remain untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reduce layout.tsx and page.tsx to minimal valid placeholders</name>
  <read_first>
    - src/app/layout.tsx (currently imports `@once-ui-system/*`, `RouteGuard`, `Providers`, `Header`, `Footer`, `@/resources/*` — all deleted in Task 1)
    - src/app/page.tsx (currently imports Once UI components and resources)
    - .planning/phases/01-foundation/01-PATTERNS.md (lines 147–222 — final shape for layout/page is built in PLAN 02; this task only produces a buildable interim state)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-20 — page.tsx is a placeholder for Phase 1)
  </read_first>
  <action>
    Rewrite `src/app/layout.tsx` to a minimal valid Next.js App Router root layout: a default-exported `RootLayout({ children }: { children: React.ReactNode })` returning `<html lang="en"><body>{children}</body></html>`. No imports of Once UI, no `RouteGuard`, no `Providers`, no `Header`, no `Footer`, no resources, no fonts yet (those land in PLAN 02 Task 2), no global CSS imports yet (those land in PLAN 02 Task 1). Plain React. No metadata object yet (PLAN 02 may stub one).

    Rewrite `src/app/page.tsx` per D-20 to a one-line placeholder: default-exported `Home` returning a single `<main>` element with inline `padding: 120px 80px` style and one paragraph containing the literal text `Foundation in place. See ` followed by an `<a>` linking to `/styleguide` with link text `/styleguide`. No imports of Once UI, no resources. Plain React.

    These are interim shapes intended only to let the project build cleanly after the strip. PLAN 02 expands `layout.tsx` with fonts + globals + chrome. PLAN 04 / Phase 2 may revisit `page.tsx`.

    Do NOT touch `src/app/not-found.tsx` in this task. It may still import Once UI — if so, also reduce it to a minimal default export returning `<div>404</div>` with no Once UI imports so the build does not break. Phase 5 RT-04 rebuilds it properly.

    Do NOT add `"use client"` to either file — both must be server components.
  </action>
  <verify>
    <automated>node -e "const a=require('fs').readFileSync('src/app/layout.tsx','utf8');const b=require('fs').readFileSync('src/app/page.tsx','utf8');if(a.includes('@once-ui-system'))process.exit(1);if(b.includes('@once-ui-system'))process.exit(1);if(!b.includes('/styleguide'))process.exit(1);if(!a.includes('RootLayout')&&!a.includes('default function'))process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/app/layout.tsx` does not contain the string `@once-ui-system`
    - source: `src/app/layout.tsx` does not contain the string `RouteGuard`
    - source: `src/app/layout.tsx` does not contain the string `Providers`
    - source: `src/app/layout.tsx` does not contain the string `@/resources`
    - source: `src/app/layout.tsx` contains a default-exported function returning JSX with `<html` and `<body`
    - source: `src/app/page.tsx` does not contain the string `@once-ui-system`
    - source: `src/app/page.tsx` does not contain the string `@/resources`
    - source: `src/app/page.tsx` contains the substring `/styleguide`
    - source: `src/app/not-found.tsx` (if it exists) does not contain the string `@once-ui-system`
  </acceptance_criteria>
  <done>
    `src/app/layout.tsx` and `src/app/page.tsx` are valid TypeScript + JSX with no references to deleted modules. They compile in isolation; the only remaining build risk is the dependency removal in Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reset src/components/index.ts and clean next.config.mjs + package.json</name>
  <read_first>
    - src/components/index.ts (currently exports `Mailchimp`, `RouteGuard`, `ThemeToggle`, `Header`, `Footer`, `FooterCTA`, etc. — all deleted in Task 1)
    - next.config.mjs (currently has `sassOptions`, `transpilePackages: ["next-mdx-remote"]`, `images.remotePatterns` for `www.google.com`)
    - package.json (deps to remove: `@once-ui-system/core`, `cookie`, `@types/cookie`, `transliteration`, `react-icons`, `react-rough-notation`, `sass`, `next-mdx-remote`, `tzdata`)
    - .planning/phases/01-foundation/01-PATTERNS.md (lines 604–668 — exact target shapes for next.config and package.json)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-05, D-11, D-16 — npm chosen as package manager since `package-lock.json` is committed)
  </read_first>
  <action>
    Rewrite `src/components/index.ts` to a minimal placeholder: a single comment line `// Phase 1 barrel — populated by PLAN 02 (chrome) and PLAN 03 (primitives)` followed by `export {};` to keep TypeScript happy as a module. No exports of deleted components.

    Rewrite `next.config.mjs` to match the skeleton in PATTERNS.md lines 614–626: import `mdx from "@next/mdx"`, define `withMDX = mdx({ extension: /\.mdx?$/, options: {} })`, export `withMDX(nextConfig)` where `nextConfig` only declares `pageExtensions: ["ts", "tsx", "md", "mdx"]`. Remove the `sassOptions` block (D-11), remove `transpilePackages: ["next-mdx-remote"]` (D-05 chose `@next/mdx` per D-16), remove `images.remotePatterns` (the `www.google.com` entry is a leftover; Phase 3 adds back if needed).

    Edit `package.json` deps (D-05, D-11, D-16):
    - Remove from `dependencies`: `@once-ui-system/core`, `cookie`, `next-mdx-remote`, `react-icons`, `react-rough-notation`, `sass`, `transliteration`.
    - Remove from `devDependencies`: `@types/cookie`, `tzdata`.
    - Keep in `dependencies`: `@mdx-js/loader`, `@next/mdx`, `classnames`, `gray-matter`, `lint-staged`, `next`, `react`, `react-dom`.
    - Keep in `devDependencies`: `@biomejs/biome`, `@eslint/eslintrc`, `@types/node`, `@types/react`, `@types/react-dom`, `eslint`, `eslint-config-next`, `typescript`.
    - Rename `"name"` from `"@once-ui-system/magic-portfolio"` to `"farzaneh-portfolio"`.
    - Remove the `export` script (`"export": "next export"`) — Next 16 does not support `next export` and this is a leftover.
    - Keep all other scripts (`dev`, `build`, `biome-write`, `start`, `lint`) verbatim.

    Then run `npm install` to regenerate `package-lock.json` against the cleaned manifest. Commit the new lockfile alongside `package.json`.
  </action>
  <verify>
    <automated>npm install --no-audit --no-fund 2>&1 | findstr /v "deprecated" & node -e "const p=require('./package.json');if(p.dependencies['@once-ui-system/core'])process.exit(1);if(p.dependencies['next-mdx-remote'])process.exit(1);if(p.dependencies['sass'])process.exit(1);if(p.dependencies['react-icons'])process.exit(1);if(p.dependencies['cookie'])process.exit(1);if(p.dependencies['transliteration'])process.exit(1);if(p.dependencies['react-rough-notation'])process.exit(1);if(!p.dependencies['@next/mdx'])process.exit(1);if(!p.dependencies['next'])process.exit(1);if(p.name!=='farzaneh-portfolio')process.exit(1);const c=require('fs').readFileSync('next.config.mjs','utf8');if(c.includes('sassOptions'))process.exit(1);if(c.includes('next-mdx-remote'))process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - config: `package.json` does not list `@once-ui-system/core` in dependencies
    - config: `package.json` does not list `next-mdx-remote` in dependencies
    - config: `package.json` does not list `sass` in dependencies
    - config: `package.json` does not list `react-icons` in dependencies
    - config: `package.json` does not list `cookie` in dependencies
    - config: `package.json` does not list `transliteration` in dependencies
    - config: `package.json` does not list `react-rough-notation` in dependencies
    - config: `package.json` does not list `@types/cookie` in devDependencies
    - config: `package.json` does not list `tzdata` in devDependencies
    - config: `package.json` lists `@next/mdx` in dependencies
    - config: `package.json` lists `gray-matter` in dependencies
    - config: `package.json` lists `classnames` in dependencies
    - source: `package.json` has `"name": "farzaneh-portfolio"`
    - source: `next.config.mjs` does not contain the string `sassOptions`
    - source: `next.config.mjs` does not contain the string `next-mdx-remote`
    - source: `next.config.mjs` does not contain the string `www.google.com`
    - source: `next.config.mjs` contains `pageExtensions` and `withMDX`
    - source: `src/components/index.ts` does not contain `Mailchimp` or `RouteGuard` or `Header` or `ThemeToggle`
    - command: `npm install` completes with exit code 0
    - source: `package-lock.json` exists and has been regenerated after the dependency changes
  </acceptance_criteria>
  <done>
    `node_modules/` resolves the cleaned dependency tree with no `@once-ui-system` resolution. `next.config.mjs` is the minimal MDX-enabled config. The barrel `src/components/index.ts` exports nothing yet, ready to be repopulated by PLAN 02 and PLAN 03.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify clean build and lint pass against stripped scaffold</name>
  <read_first>
    - package.json (scripts: `build`, `lint`)
    - .planning/phases/01-foundation/01-CONTEXT.md (INFRA-01 requires the scaffold to compile after the strip)
  </read_first>
  <action>
    Run `npm run build` from the project root. The build must succeed with the stripped scaffold (no Once UI, no deleted route handlers, minimal `layout.tsx` and `page.tsx`). If the build surfaces any residual import of a deleted file (e.g., a stray reference inside `src/app/not-found.tsx`, `src/app/robots.ts`, or `src/app/sitemap.ts`), edit only the offending file to remove the dead import — do not reintroduce deleted modules.

    Run `npm run lint`. Resolve any lint errors that originate from the strip (typically unused imports left behind in `robots.ts` or `sitemap.ts`). Do not silence lint with global disables.

    Do NOT add new components, fonts, styles, or chrome in this task — that is PLAN 02's responsibility. The deliverable here is "stripped scaffold builds and lints cleanly."

    Capture the build output and the post-build `out/` or `.next/` size as a sanity check for the SUMMARY.
  </action>
  <verify>
    <automated>npm run build 2>&1 && npm run lint 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - command: `npm run build` exits 0
    - command: `npm run lint` exits 0
    - source: build output contains a route entry for `/` and either `/_not-found` or the kept `not-found` page; no entries for deleted routes (`/blog`, `/gallery`, `/work`, `/api/authenticate`, `/api/check-auth`, `/api/og/*`, `/api/rss`)
    - behavior: project compiles end-to-end against the cleaned dependency tree
  </acceptance_criteria>
  <done>
    `next build` succeeds, `eslint` passes. The scaffold is a clean foundation that PLAN 02 can rebuild on. INFRA-01 ("project cleaned up") is satisfied in observable, automated form.
  </done>
</task>

</tasks>

<verification>
After all four tasks: `npm run build` and `npm run lint` both exit 0; the working tree no longer contains any of the paths enumerated in PATTERNS.md "Deletions"; `package.json` and `next.config.mjs` match the target shapes in PATTERNS.md sections "package.json (rewrite)" and "next.config.mjs (rewrite)".
</verification>

<success_criteria>
- INFRA-01 satisfied: Next.js project cleaned up; unused legacy components, routes, and dependencies removed.
- Repo is in a buildable, lintable state with the minimum viable scaffold.
- PLAN 02 can begin without negotiating deletions or dep removals.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` summarizing: deletions performed, deps removed, build/lint result, any deviations (e.g., a file that had to also be touched to keep the build green).
</output>
