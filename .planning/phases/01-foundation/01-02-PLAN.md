---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/styles/tokens.css
  - src/styles/base.css
  - src/app/layout.tsx
  - src/components/chrome/BrandMark.tsx
  - src/components/chrome/BrandMark.module.css
  - src/components/chrome/FloatingDock.tsx
  - src/components/chrome/FloatingDock.module.css
  - src/components/index.ts
  - src/app/styleguide/page.tsx
  - src/app/styleguide/page.module.css
  - src/app/styleguide/mdx-test/page.mdx
autonomous: true
requirements: [DS-01, DS-02, DS-03, INFRA-02]
must_haves:
  truths:
    - "Visiting `/styleguide` renders a real Next.js page with cream `#ebe8e2` background, ink `#1a1a1a` text, Instrument Serif + Inter fonts loaded with no FOUT"
    - "Four stub anchor sections (`#hero`, `#work`, `#about`, `#contact`) render in document order on `/styleguide`, each tall enough to scroll between"
    - "A static `Farzaneh` brand mark in italic Instrument Serif is fixed top-left on `/styleguide` (no inversion behavior yet — PLAN 04 adds it)"
    - "A static centered-bottom pill dock with anchor links `Home`/`Work`/`About`/`Contact` + `Get in touch ↗` CTA is visible on `/styleguide` (no scroll/active behavior yet — PLAN 04 adds it)"
    - "Visiting `/styleguide/mdx-test` renders an MDX document end-to-end (heading + paragraph + `<em>`) proving `@next/mdx` is wired"
    - "Tokens defined in `tokens.css` are reachable from CSS Modules via `var(--bg)`, `var(--ink)`, `var(--accent)`, `var(--font-serif)`, `var(--font-sans)`, etc."
  artifacts:
    - path: "src/styles/tokens.css"
      provides: "Global CSS custom properties for colors, surfaces, and font families"
      contains: "--bg: #ebe8e2; --ink: #1a1a1a; --accent: #ff5a36; --font-serif (overridden with --font-instrument-serif); --font-sans (overridden with --font-inter)"
    - path: "src/styles/base.css"
      provides: "Global reset and body defaults"
      contains: "box-sizing reset, html/body overflow-x: hidden, body font-family: var(--font-sans), body background: var(--bg), body color: var(--ink)"
    - path: "src/app/layout.tsx"
      provides: "Root layout with next/font, global CSS, BrandMark, FloatingDock mounted once"
      contains: "Instrument_Serif and Inter from next/font/google, variable: '--font-instrument-serif' and '--font-inter', imports of tokens.css and base.css, <BrandMark /> and <FloatingDock /> wrapping {children}"
    - path: "src/components/chrome/BrandMark.tsx"
      provides: "Stub brand mark (final mix-blend-difference behavior in PLAN 04)"
      contains: "default export rendering <div className={styles.brand}>Farzaneh</div>; server component (no \"use client\")"
    - path: "src/components/chrome/FloatingDock.tsx"
      provides: "Stub floating dock (final IntersectionObserver/scroll-hide behavior in PLAN 04)"
      contains: "default export rendering <nav> with anchor links to #hero, #work, #about, #contact + CTA anchor; client component scaffold acceptable but no behavior yet"
    - path: "src/app/styleguide/page.tsx"
      provides: "Styleguide skeleton with stub anchor sections"
      contains: "four <section> elements with ids hero, work, about, contact; each containing placeholder text rendered with current globals"
    - path: "src/app/styleguide/mdx-test/page.mdx"
      provides: "MDX proof of life (INFRA-02)"
      contains: "an h1, a paragraph, and an `<em>` element"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/styles/tokens.css and src/styles/base.css"
      via: "ES module import at the top of layout.tsx"
      pattern: "import .*tokens\\.css|import .*base\\.css"
    - from: "src/app/layout.tsx"
      to: "next/font CSS variables -> tokens.css overrides"
      via: "html element gets className with both --font-instrument-serif and --font-inter variable classes; tokens.css uses var(--font-instrument-serif)/var(--font-inter) inside --font-serif/--font-sans"
      pattern: "instrumentSerif\\.variable|inter\\.variable"
    - from: "src/app/layout.tsx"
      to: "src/components/chrome/BrandMark and FloatingDock"
      via: "JSX mount inside <body> alongside {children}"
      pattern: "<BrandMark|<FloatingDock"
    - from: "src/app/styleguide/mdx-test/page.mdx"
      to: "@next/mdx loader configured in next.config.mjs"
      via: "pageExtensions includes 'mdx' (set by PLAN 01)"
      pattern: "page\\.mdx"
---

<objective>
Deliver the walking-skeleton vertical slice: tokens.css + base.css load via a rebuilt `layout.tsx` that also mounts `next/font` typography, a stub `BrandMark`, and a stub `FloatingDock`. A new `/styleguide` route renders four stub anchor sections that are tall enough to scroll between, demonstrating the foundation works end-to-end. An MDX document at `/styleguide/mdx-test` proves `@next/mdx` is wired.

This is the lowest-wave plan after the strip and serves as the **Walking Skeleton** (per WALKING_SKELETON=true + MVP_MODE=true). After this plan, the thinnest end-to-end stack is in place. PLAN 03 swaps stub markup inside `/styleguide` sections for real primitives. PLAN 04 swaps stub chrome for the full BrandMark + FloatingDock behaviors.

Purpose: Honor D-07..D-12, D-16, D-17, D-18, D-20 in a single coherent slice; satisfy DS-01 (typography loads with no FOUT), DS-02 (color tokens exist as CSS variables consumed by globals), DS-03 (spacing/radius approach decided — tokens.css holds color/typography vars; spacing/radius literals live in component CSS unless usage justifies promoting), and INFRA-02 (MDX renders end-to-end).

Output: Production-buildable skeleton with `/styleguide` reachable and visible, fonts loaded, globals applied, MDX proven.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-SKELETON.md
@.planning/sketches/006-full-home/index.html

<interfaces>
<!-- Binding visual + behavioral spec: .planning/sketches/006-full-home/index.html -->
<!-- Tokens to lift verbatim: lines 11–27 (cream/ink/accent vars + font-family vars) -->
<!-- Reset to lift verbatim: lines 28–38 (box-sizing reset, html/body overflow, body defaults) -->
<!-- Brand mark CSS to lift verbatim: lines 55–61 (.brand class — fixed top-left, italic Instrument Serif 22px). PLAN 04 will replace `color: var(--ink)` with mix-blend-difference (D-14 Path A). This plan delivers the stub with `color: var(--ink)` only — no behavior. -->
<!-- Dock CSS to lift verbatim: lines 66–87 (.dock + .dock a + .dock-sep + .dock-cta + hidden state). This plan delivers the stub markup and CSS only — no behavior. -->
<!-- After PLAN 01, package.json scripts: `npm run dev`, `npm run build`, `npm run lint`. Package manager: npm. -->

<!-- next/font usage pattern from PATTERNS.md lines 162–198 (rewritten layout.tsx skeleton) -->
<!-- - Instrument_Serif: weight "400", style ["normal", "italic"], variable: "--font-instrument-serif", display: "swap", preload: true -->
<!-- - Inter: weight ["300", "400", "500", "600"], variable: "--font-inter", display: "swap", preload: true -->
<!-- - html className combines both .variable classes -->

<!-- D-14 lockdown (planner-resolved): BrandMark uses mix-blend-difference (Path A). Plan 02 ships the stub (no inversion); Plan 04 implements full Path A. -->
<!-- D-16 lockdown (planner-resolved): MDX library is @next/mdx, file-based routes. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create global tokens.css and base.css lifted from sketch</name>
  <read_first>
    - .planning/sketches/006-full-home/index.html (lines 11–27 — `:root` token block; lines 28–38 — global reset)
    - .planning/phases/01-foundation/01-PATTERNS.md (sections "src/styles/tokens.css" lines 91–123 and "src/styles/base.css" lines 126–143)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-07, D-08, D-09, D-10)
  </read_first>
  <action>
    Create `src/styles/tokens.css` containing a `:root` block with CSS custom properties lifted verbatim from sketch lines 11–27: `--bg: #ebe8e2`, `--bg-2: #e3dfd6`, `--bg-3: #dcd6c9`, `--ink: #1a1a1a`, `--ink-2: #2a2a2a`, `--muted: #5a5a5a`, `--muted-2: #8a8a85`, `--line: #d5d0c5`, `--accent: #ff5a36`, `--accent-soft: #ffd8c8`, `--surface: #ffffff`, `--dark: #1a1612`.

    For `--font-serif` and `--font-sans`, apply the override pattern from PATTERNS.md lines 117–120: `--font-serif: var(--font-instrument-serif), 'Instrument Serif', serif;` and `--font-sans: var(--font-inter), 'Inter', system-ui, sans-serif;`. The `--font-instrument-serif` / `--font-inter` variables are produced by `next/font` in Task 2 and made available by the className applied to `<html>`.

    Per D-03/DS-03 spacing/radius decision: do NOT introduce `--space-*` or `--radius-*` tokens in tokens.css. Component-level CSS Modules will use literal pixel values from the sketch (e.g., `padding: 10px 22px` for dock anchors, `border-radius: 100px` for pill shapes, section padding `140px 80px 100px` for hero). This matches the sketch's CSS-driven approach and avoids inventing a token scale that the sketch did not define.

    Create `src/styles/base.css` lifted verbatim from sketch lines 28–38: the universal `*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }` reset, `html, body { width: 100%; overflow-x: hidden; }`, body defaults (`font-family: var(--font-sans)`, `background: var(--bg)`, `color: var(--ink)`, `min-height: 100vh`, `-webkit-font-smoothing: antialiased`), anchor reset (`text-decoration: none; color: inherit;`), button reset (`font-family: inherit; cursor: pointer; border: none; background: none;`).

    These two files are imported by `layout.tsx` in Task 2.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('src/styles/tokens.css','utf8');const b=require('fs').readFileSync('src/styles/base.css','utf8');const need=['--bg:','#ebe8e2','--ink:','#1a1a1a','--accent:','#ff5a36','--font-serif:','--font-sans:','--font-instrument-serif','--font-inter'];for(const k of need)if(!t.includes(k)){console.error('tokens missing',k);process.exit(1)};const bneed=['box-sizing: border-box','overflow-x: hidden','var(--font-sans)','var(--bg)','var(--ink)'];for(const k of bneed)if(!b.includes(k)){console.error('base missing',k);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/styles/tokens.css` exists and contains `--bg: #ebe8e2`
    - source: `src/styles/tokens.css` contains `--ink: #1a1a1a`
    - source: `src/styles/tokens.css` contains `--accent: #ff5a36`
    - source: `src/styles/tokens.css` contains `--bg-2`, `--bg-3`, `--ink-2`, `--muted`, `--muted-2`, `--line`, `--accent-soft`, `--surface`, `--dark`
    - source: `src/styles/tokens.css` contains `--font-serif:` referencing `var(--font-instrument-serif)`
    - source: `src/styles/tokens.css` contains `--font-sans:` referencing `var(--font-inter)`
    - source: `src/styles/tokens.css` does NOT contain `--space-` (spacing/radius tokens are intentionally not promoted)
    - source: `src/styles/base.css` exists and contains `box-sizing: border-box`
    - source: `src/styles/base.css` contains `overflow-x: hidden`
    - source: `src/styles/base.css` references `var(--font-sans)`, `var(--bg)`, `var(--ink)`
  </acceptance_criteria>
  <done>
    Two global CSS files exist matching sketch lines 11–27 and 28–38 verbatim (with the documented font-variable override). They are not yet loaded — Task 2 wires them into `layout.tsx`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite layout.tsx with next/font + globals + chrome mounts</name>
  <read_first>
    - src/app/layout.tsx (minimal placeholder from PLAN 01 Task 2)
    - .planning/phases/01-foundation/01-PATTERNS.md (lines 147–204 — full target layout.tsx skeleton)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-10, D-12)
    - .planning/sketches/006-full-home/index.html (lines 367, 371–380 — brand text "Farzaneh" and dock copy "Get in touch ↗" for the stub markup Task 3 creates)
  </read_first>
  <action>
    Rewrite `src/app/layout.tsx` matching the skeleton in PATTERNS.md lines 162–198. The file is a server component (no `"use client"`).

    Imports at the top: from `next/font/google` import `Instrument_Serif` and `Inter`; import `"@/styles/tokens.css"` and `"@/styles/base.css"`; import `BrandMark` from `@/components/chrome/BrandMark` and `FloatingDock` from `@/components/chrome/FloatingDock` (both created in Task 3 below).

    Configure `instrumentSerif = Instrument_Serif({ subsets: ["latin"], weight: "400", style: ["normal", "italic"], variable: "--font-instrument-serif", display: "swap", preload: true })`. Configure `inter = Inter({ subsets: ["latin"], weight: ["300", "400", "500", "600"], variable: "--font-inter", display: "swap", preload: true })`.

    Export a default `RootLayout({ children }: { children: React.ReactNode })` returning `<html lang="en" className={`${instrumentSerif.variable} ${inter.variable}`}><body><BrandMark />{children}<FloatingDock /></body></html>`.

    Add an exported `metadata` constant (`export const metadata: Metadata = { title: "Farzaneh Falakrou", description: "Product designer portfolio." }`) — minimal stub. Phase 5 SEO-01 expands this.

    Do NOT import any Once UI module. Do NOT add a top-level `<Providers>` or `<RouteGuard>`. Do NOT inline a theme-init script.

    `display: "swap"` + `preload: true` is what eliminates FOUT per DS-01 / Roadmap SC #1.
  </action>
  <verify>
    <automated>node -e "const a=require('fs').readFileSync('src/app/layout.tsx','utf8');for(const k of ['next/font/google','Instrument_Serif','Inter','--font-instrument-serif','--font-inter','@/styles/tokens.css','@/styles/base.css','BrandMark','FloatingDock','display: \"swap\"','preload: true','instrumentSerif.variable','inter.variable']){if(!a.includes(k)){console.error('layout missing',k);process.exit(1)}}if(a.includes('@once-ui-system'))process.exit(1);if(a.includes('\"use client\"'))process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/app/layout.tsx` imports `Instrument_Serif` and `Inter` from `next/font/google`
    - source: `src/app/layout.tsx` configures Instrument Serif with `variable: "--font-instrument-serif"`, `display: "swap"`, `preload: true`, `weight: "400"`, `style: ["normal", "italic"]`
    - source: `src/app/layout.tsx` configures Inter with `variable: "--font-inter"`, `display: "swap"`, `preload: true`
    - source: `src/app/layout.tsx` imports `@/styles/tokens.css` and `@/styles/base.css`
    - source: `src/app/layout.tsx` imports `BrandMark` from `@/components/chrome/BrandMark`
    - source: `src/app/layout.tsx` imports `FloatingDock` from `@/components/chrome/FloatingDock`
    - source: `src/app/layout.tsx` renders `<BrandMark />` and `<FloatingDock />` inside `<body>`
    - source: `src/app/layout.tsx` applies `${instrumentSerif.variable} ${inter.variable}` className to `<html>`
    - source: `src/app/layout.tsx` does NOT contain `@once-ui-system`
    - source: `src/app/layout.tsx` does NOT contain `"use client"` at top
  </acceptance_criteria>
  <done>
    Root layout loads typography via next/font, applies CSS variables to `<html>` so `tokens.css` font-family overrides resolve, imports both global CSS files, and mounts the (stub) chrome components from Task 3. The layout is ready to render `/styleguide` correctly once Task 3 and Task 4 land.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create stub BrandMark and stub FloatingDock chrome components</name>
  <read_first>
    - .planning/sketches/006-full-home/index.html (lines 55–61 — `.brand` CSS; lines 66–87 — `.dock` CSS; line 369 — brand text "Farzaneh"; line 378 — CTA copy "Get in touch ↗")
    - .planning/phases/01-foundation/01-PATTERNS.md (sections "BrandMark.tsx + BrandMark.module.css" lines 359–422 and "FloatingDock.tsx + FloatingDock.module.css" lines 425–528)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-14 planner-locked to Path A in PLAN 04; D-15 dock IDs `hero`, `work`, `about`, `contact`)
  </read_first>
  <action>
    Create `src/components/chrome/BrandMark.tsx` as a server component (no `"use client"`). Default-export a function `BrandMark()` that returns `<div className={styles.brand}>Farzaneh</div>`. Import `styles from "./BrandMark.module.css"`. The literal brand text is `Farzaneh` per sketch line 369. **No mix-blend-difference yet** — that ships in PLAN 04. The stub uses `color: var(--ink)` so it is legible over the cream background on `/styleguide`.

    Create `src/components/chrome/BrandMark.module.css` containing the `.brand` rule lifted from sketch lines 55–61: `position: fixed; top: 60px; left: 40px; z-index: 60; font-family: var(--font-serif); font-style: italic; font-size: 22px; color: var(--ink); transition: color 0.3s;`. Do NOT add the `mix-blend-mode: difference` declaration in this plan — PLAN 04 owns that change.

    Create `src/components/chrome/FloatingDock.tsx` with a `"use client"` directive at the top (because PLAN 04 will add hooks; introducing the client boundary now avoids churning the module type later, and a client component with no hooks is fine). Default-export a function `FloatingDock()` that returns a `<nav className={styles.dock}>` containing four anchor links in document order — `<a href="#hero">Home</a>`, `<a href="#work">Work</a>`, `<a href="#about">About</a>`, `<a href="#contact">Contact</a>` — followed by `<div className={styles.sep} />` and a final `<a href="#contact" className={styles.cta}>Get in touch ↗</a>`. Native anchor behavior (browser jump) is acceptable in the stub; PLAN 04 replaces with smooth-scroll. No `IntersectionObserver`, no scroll listener, no `hidden` state — those land in PLAN 04.

    Create `src/components/chrome/FloatingDock.module.css` lifting the rules from sketch lines 66–87 verbatim: `.dock`, `.dock.hidden` (include the class so PLAN 04 can toggle it without re-touching CSS), `.dock a`, `.dock a:hover`, `.dock a.active`, `.dock-sep` (rename selector to `.sep` to match the module class), `.dock-cta` (rename to `.cta`), `.dock-cta:hover` (becomes `.cta:hover`). Keep all literal pixel values, colors, and timings from the sketch (e.g., `padding: 10px 22px`, `border-radius: 100px`, `background: rgba(26,26,26,0.92)`, `backdrop-filter: blur(10px)`, `box-shadow: 0 8px 32px rgba(0,0,0,0.18)`, `transform: translate(-50%, 120px)` for hidden, transitions `0.3s ease` for the container and `0.15s` for anchors).

    Update `src/components/index.ts` (currently the placeholder from PLAN 01 Task 3) to re-export chrome:
    `export { BrandMark } from "./chrome/BrandMark"; export { FloatingDock } from "./chrome/FloatingDock";`
    Do not yet add primitive exports — those land in PLAN 03.
  </action>
  <verify>
    <automated>node -e "const b=require('fs').readFileSync('src/components/chrome/BrandMark.tsx','utf8');const bc=require('fs').readFileSync('src/components/chrome/BrandMark.module.css','utf8');const d=require('fs').readFileSync('src/components/chrome/FloatingDock.tsx','utf8');const dc=require('fs').readFileSync('src/components/chrome/FloatingDock.module.css','utf8');const i=require('fs').readFileSync('src/components/index.ts','utf8');if(!b.includes('Farzaneh'))process.exit(1);if(b.includes('mix-blend'))process.exit(1);if(b.includes('\"use client\"'))process.exit(1);if(!bc.includes('position: fixed')||!bc.includes('top: 60px')||!bc.includes('left: 40px')||!bc.includes('var(--font-serif)')||!bc.includes('font-style: italic')||!bc.includes('22px'))process.exit(1);if(bc.includes('mix-blend-mode'))process.exit(1);if(!d.includes('\"use client\"'))process.exit(1);for(const id of ['#hero','#work','#about','#contact'])if(!d.includes(id))process.exit(1);if(!d.includes('Get in touch'))process.exit(1);if(d.includes('IntersectionObserver'))process.exit(1);if(d.includes('addEventListener'))process.exit(1);if(!dc.includes('position: fixed')||!dc.includes('bottom: 24px')||!dc.includes('border-radius: 100px')||!dc.includes('backdrop-filter'))process.exit(1);if(!i.includes('BrandMark')||!i.includes('FloatingDock'))process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/components/chrome/BrandMark.tsx` exports a function rendering the literal text `Farzaneh`
    - source: `src/components/chrome/BrandMark.tsx` does NOT contain `"use client"`
    - source: `src/components/chrome/BrandMark.tsx` does NOT contain `mix-blend` (PLAN 04 adds it)
    - source: `src/components/chrome/BrandMark.module.css` contains `position: fixed`, `top: 60px`, `left: 40px`, `z-index: 60`, `font-family: var(--font-serif)`, `font-style: italic`, `font-size: 22px`, `color: var(--ink)`
    - source: `src/components/chrome/BrandMark.module.css` does NOT contain `mix-blend-mode` (PLAN 04 adds it)
    - source: `src/components/chrome/FloatingDock.tsx` contains `"use client"` at top
    - source: `src/components/chrome/FloatingDock.tsx` renders anchor links with `href="#hero"`, `href="#work"`, `href="#about"`, `href="#contact"`
    - source: `src/components/chrome/FloatingDock.tsx` contains the literal text `Get in touch ↗`
    - source: `src/components/chrome/FloatingDock.tsx` does NOT contain `IntersectionObserver` (PLAN 04 adds it)
    - source: `src/components/chrome/FloatingDock.tsx` does NOT contain `addEventListener` (PLAN 04 adds scroll listener)
    - source: `src/components/chrome/FloatingDock.module.css` contains `position: fixed`, `bottom: 24px`, `border-radius: 100px`, `backdrop-filter: blur(10px)`
    - source: `src/components/chrome/FloatingDock.module.css` contains a `.dock.hidden` rule with `transform: translate(-50%, 120px)`
    - source: `src/components/index.ts` exports `BrandMark` and `FloatingDock`
  </acceptance_criteria>
  <done>
    Two stub chrome components and their CSS modules exist with sketch-faithful visuals but no behavior. `layout.tsx` (Task 2) can import them without error. PLAN 04 will swap their internals for the full behavior without renaming files.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create /styleguide route with stub anchor sections + MDX proof page</name>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (lines 532–579 — styleguide page skeleton; lines 583–600 — MDX proof of life)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-17, D-18, D-19)
    - .planning/sketches/006-full-home/index.html (any section padding patterns, e.g., `.hero` lines 89–92, `.work` lines 167–170, `.contact` lines 290–294)
  </read_first>
  <action>
    Create `src/app/styleguide/page.tsx` as a server component (no `"use client"`). Default-export `StyleguidePage()` returning a `<main>` containing four `<section>` elements in this exact order with these exact ids: `hero`, `work`, `about`, `contact`. Each section uses `className={styles.section}` from a colocated `page.module.css`. Inside each section, put a placeholder `<h1>` (or `<h2>`) text identifying the section (e.g., `Hero placeholder`, `Work placeholder`, `About placeholder`, `Contact placeholder`) and one paragraph of lorem-style filler about the section's purpose. **Do not import primitives in this plan — that is PLAN 03 Task 4.** The placeholder text uses default browser styling against `tokens.css` + `base.css`, which is enough to verify fonts loaded and the cream background renders.

    Each section must be tall enough for the dock to scroll between (PLAN 04 will rely on this). Apply `min-height: 100vh` to `.section` in `page.module.css`. Also set per-section padding lifted from sketch patterns: hero `padding: 140px 80px 100px`; work `padding: 60px 80px 100px`; about `padding: 120px 80px`; contact `padding: 120px 80px`. Width container: `max-width: 1500px; margin: 0 auto;` on `.section` to mirror sketch `body` lines 39–41.

    Mark `#about` and `#contact` sections with the attribute `data-darken`. In `page.module.css`, target dark sections via the attribute selector `section[data-darken]` to apply `background: var(--ink); color: var(--bg);`. This is the brand-mark inversion test surface (PLAN 04 verifies mix-blend-difference works against these regions).

    Create `src/app/styleguide/mdx-test/page.mdx` per D-17 / INFRA-02. Content (verbatim minimum):
    `# MDX proof of life`
    `This document renders end-to-end via @next/mdx with *italic emphasis* on a single word.`
    `The presence of this route at /styleguide/mdx-test means @next/mdx is wired into the App Router.`

    No frontmatter (Phase 3 adds frontmatter usage). No custom MDX component map (Phase 3 adds one). Default MDX renders standard HTML; `base.css` styles the body font and color so the MDX page inherits Inter + ink-on-cream.

    Do NOT add a link from `/` or main navigation to `/styleguide` per D-19. The route is discoverable only by typing the URL (and from the placeholder text in `src/app/page.tsx` written in PLAN 01 Task 2).
  </action>
  <verify>
    <automated>node -e "const p=require('fs').readFileSync('src/app/styleguide/page.tsx','utf8');const c=require('fs').readFileSync('src/app/styleguide/page.module.css','utf8');const m=require('fs').readFileSync('src/app/styleguide/mdx-test/page.mdx','utf8');for(const id of ['id=\"hero\"','id=\"work\"','id=\"about\"','id=\"contact\"'])if(!p.includes(id))process.exit(1);if(!p.includes('data-darken'))process.exit(1);if(p.includes('\"use client\"'))process.exit(1);if(p.includes('@/components/primitives'))process.exit(1);for(const k of ['min-height: 100vh','max-width: 1500px','background: var(--ink)','color: var(--bg)','data-darken'])if(!c.includes(k))process.exit(1);if(!m.includes('# ')||!m.includes('*'))process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/app/styleguide/page.tsx` exports a default function rendering four `<section>` elements with `id="hero"`, `id="work"`, `id="about"`, `id="contact"` in document order
    - source: `src/app/styleguide/page.tsx` applies `data-darken` to the `#about` and `#contact` sections
    - source: `src/app/styleguide/page.tsx` does NOT import from `@/components/primitives` (PLAN 03 wires those)
    - source: `src/app/styleguide/page.tsx` does NOT contain `"use client"`
    - source: `src/app/styleguide/page.module.css` contains `min-height: 100vh`
    - source: `src/app/styleguide/page.module.css` contains `max-width: 1500px`
    - source: `src/app/styleguide/page.module.css` contains a rule targeting `[data-darken]` with `background: var(--ink)` and `color: var(--bg)`
    - source: `src/app/styleguide/mdx-test/page.mdx` exists with a top-level `# ` heading and at least one `*…*` italic span
    - command: `npm run build` exits 0 with `/styleguide` and `/styleguide/mdx-test` in the build output route list
    - behavior: starting `npm run dev` and navigating to `http://localhost:3000/styleguide` returns HTTP 200 and the rendered HTML contains the literal text `Farzaneh`, `Get in touch ↗`, `Hero placeholder`, and `Contact placeholder`
    - behavior: navigating to `http://localhost:3000/styleguide/mdx-test` returns HTTP 200 with the literal text `MDX proof of life` and an `<em>` element in the response HTML
  </acceptance_criteria>
  <done>
    `/styleguide` renders a real Next.js page with four anchor sections, the stub brand mark visible top-left, the stub dock visible center-bottom, cream background, Instrument Serif and Inter loaded, no FOUT. `/styleguide/mdx-test` renders an MDX document end-to-end. The walking skeleton is live.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Visual verification of walking skeleton</name>
  <what-built>
    Walking skeleton end-to-end stack: globals load via layout.tsx, next/font typography active, `/styleguide` renders with stub anchor sections, stub BrandMark fixed top-left, stub FloatingDock fixed center-bottom, MDX proof at `/styleguide/mdx-test`.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit `http://localhost:3000/styleguide`.
    2. Confirm cream background `#ebe8e2`, body text in Inter, the "Farzaneh" mark in italic Instrument Serif top-left at roughly `top: 60px; left: 40px`.
    3. Confirm a centered-bottom pill dock is visible with anchor links `Home`, `Work`, `About`, `Contact`, a thin separator, and a `Get in touch ↗` CTA pill. Clicking any anchor causes a default-browser jump to the corresponding section (smooth-scroll is PLAN 04).
    4. Scroll the page. The Hero and Work sections render cream-on-ink. The About and Contact sections render dark (`#1a1a1a` background, cream text) because they carry `data-darken`. The brand mark does NOT yet invert over dark sections — that is PLAN 04's job and is the next checkpoint.
    5. Visit `http://localhost:3000/styleguide/mdx-test`. Confirm the page renders a heading, a paragraph, and an italic word — proving `@next/mdx` works.
    6. Open DevTools, switch to "Network", filter by "Font". Confirm the Instrument Serif and Inter font files load with status 200. There should be no Flash of Unstyled Text on initial paint.
  </how-to-verify>
  <resume-signal>Reply `approved` if all six verification points pass. If anything is broken (fonts not loading, dock not visible, dark sections not dark, MDX page 404s), describe the failure and which task to revisit.</resume-signal>
</task>

</tasks>

<verification>
After all five tasks: `npm run build` exits 0; `/styleguide` and `/styleguide/mdx-test` are in the build output; visual inspection in dev mode shows fonts, globals, stub chrome, and MDX all working as described.
</verification>

<success_criteria>
- DS-01 satisfied: Typography (Instrument Serif + Inter) loads via `next/font` with no FOUT (verified by Task 5 step 6).
- DS-02 satisfied: Color tokens exist as CSS variables in `tokens.css` and are consumed by `base.css` (and downstream by every component module).
- DS-03 partially satisfied: Spacing/radius decision recorded (literals in component CSS, no premature `--space-*` token scale) and applied in `/styleguide` section padding. DS-03 fully satisfied after PLAN 03 (primitives consume the same approach).
- INFRA-02 satisfied: MDX renders end-to-end at `/styleguide/mdx-test`.
- Walking skeleton ships: `/styleguide` is a real, scrollable, fonted page with chrome mounted.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` summarizing: tokens lifted from sketch, layout.tsx rebuild, chrome stubs delivered, /styleguide and MDX route live, visual verification result, any deviations.
</output>
