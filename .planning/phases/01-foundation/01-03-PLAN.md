---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/components/primitives/Eyebrow.tsx
  - src/components/primitives/Eyebrow.module.css
  - src/components/primitives/DisplayHeading.tsx
  - src/components/primitives/DisplayHeading.module.css
  - src/components/primitives/Italic.tsx
  - src/components/primitives/Italic.module.css
  - src/components/primitives/UnderlineAccent.tsx
  - src/components/primitives/UnderlineAccent.module.css
  - src/components/primitives/PulseDot.tsx
  - src/components/primitives/PulseDot.module.css
  - src/components/primitives/TagChip.tsx
  - src/components/primitives/TagChip.module.css
  - src/components/primitives/index.ts
  - src/components/index.ts
  - src/app/styleguide/page.tsx
  - src/app/styleguide/page.module.css
autonomous: true
requirements: [DS-03, DS-04]
must_haves:
  truths:
    - "All six primitives render in `/styleguide` with sketch-matching visuals: cream/ink in light sections, cream/ink-inverted in dark sections"
    - "`Eyebrow` renders both `variant=\"sans\"` (uppercase tracked label, 13px Inter, with optional `PulseDot`) and `variant=\"italic\"` (italic Instrument Serif accent, 22px)"
    - "`DisplayHeading` renders four sizes (`hero`, `work`, `about`, `contact`) with `<em>` children styled as italic accent via internal CSS"
    - "`Italic` renders `<em>` tags that inherit display-heading scaling in heading context and body-italic scaling in paragraph context"
    - "`UnderlineAccent` renders text with a 3px orange underline at 8px offset"
    - "`PulseDot` renders an 8px orange dot pulsing on a 2s loop (opacity 1 → 0.4 → 1)"
    - "`TagChip` renders a 1px outlined pill with uppercase tracked 11px label"
    - "`/styleguide` sections (`#hero`/`#work`/`#about`/`#contact`) contain at least one instance of every primitive variant"
  artifacts:
    - path: "src/components/primitives/Eyebrow.tsx"
      provides: "Eyebrow primitive with sans/italic variants, optional pulse dot"
      exports: ["Eyebrow"]
    - path: "src/components/primitives/DisplayHeading.tsx"
      provides: "DisplayHeading primitive with as/size props"
      exports: ["DisplayHeading"]
    - path: "src/components/primitives/Italic.tsx"
      provides: "Italic primitive (renders <em>)"
      exports: ["Italic"]
    - path: "src/components/primitives/UnderlineAccent.tsx"
      provides: "UnderlineAccent primitive"
      exports: ["UnderlineAccent"]
    - path: "src/components/primitives/PulseDot.tsx"
      provides: "PulseDot primitive"
      exports: ["PulseDot"]
    - path: "src/components/primitives/TagChip.tsx"
      provides: "TagChip primitive"
      exports: ["TagChip"]
    - path: "src/components/primitives/index.ts"
      provides: "Barrel re-exporting all six primitives"
      exports: ["Eyebrow", "DisplayHeading", "Italic", "UnderlineAccent", "PulseDot", "TagChip"]
  key_links:
    - from: "src/app/styleguide/page.tsx"
      to: "src/components/primitives/*"
      via: "named imports from `@/components/primitives`"
      pattern: "from \"@/components/primitives\""
    - from: "src/components/index.ts"
      to: "src/components/primitives/index.ts"
      via: "wildcard re-export"
      pattern: "export \\* from \"./primitives\""
---

<objective>
Build the six locked primitive components (Eyebrow, DisplayHeading, Italic, UnderlineAccent, PulseDot, TagChip) with colocated CSS Modules lifted verbatim from sketch 006. Wire all six (with all documented variants) into the four `/styleguide` sections so the styleguide becomes living documentation per D-18.

Purpose: Satisfy DS-04 (build shared primitive components) and complete DS-03 (spacing/radius literals consumed in component CSS as decided in PLAN 02). The primitives are the building blocks that Phases 2, 3, and 4 will compose into pages — getting them sketch-accurate here means later phases don't reinvent.

Output: Six TSX + six CSS Modules + a barrel + a styleguide page that demonstrates every primitive variant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-SKELETON.md
@.planning/sketches/006-full-home/index.html
@src/styles/tokens.css
@src/styles/base.css
@src/app/styleguide/page.tsx

<interfaces>
<!-- Sketch line ranges (binding visual spec): -->
<!-- Eyebrow sans: sketch lines 93–96 (.hero-eyebrow) -->
<!-- Eyebrow italic: sketch lines 233–236 (.about-head) and 293–296 (.contact-eyebrow) -->
<!-- DisplayHeading: sketch lines 103–109 (.hero h1) + per-size literals from .work-title (193–199), .about-title (243–248), .contact-title (300–305) -->
<!-- Italic: sketch lines 109 (.hero h1 em), 122 (.hero-sub-text em), 200 (.work-name em), 205 (.work-desc em), 247 (.about-title em), 252 (.about-text em), 303 (.contact-title em) -->
<!-- UnderlineAccent: sketch lines 110–115 (.underline-accent) -->
<!-- PulseDot: sketch lines 97–101 (.pulse-dot + @keyframes pulse) -->
<!-- TagChip: sketch lines 206–210 (.work-tag) -->

<!-- Prop shapes (planner-locked from PATTERNS.md): -->
<!-- Eyebrow: { variant?: "sans" | "italic" (default "sans"); withPulseDot?: boolean; children: React.ReactNode } -->
<!-- DisplayHeading: { as?: "h1" | "h2" | "h3"; size?: "hero" | "work" | "about" | "contact"; children: React.ReactNode } -->
<!-- Italic: { children: React.ReactNode } -- always renders <em>; parent CSS governs scaling/color -->
<!-- UnderlineAccent: { children: React.ReactNode } -->
<!-- PulseDot: no props; renders <span aria-hidden /> -->
<!-- TagChip: { children: React.ReactNode } -->

<!-- All primitives are SERVER COMPONENTS — no "use client" needed -->
<!-- All imports use the relative module convention: `import styles from "./Foo.module.css"` -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PulseDot, UnderlineAccent, TagChip, Italic (simple primitives)</name>
  <read_first>
    - .planning/sketches/006-full-home/index.html (lines 97–101 for PulseDot, lines 110–115 for UnderlineAccent, lines 206–210 for TagChip; lines 109, 122, 200, 205, 247, 252, 303 for Italic's `em` rules in context)
    - .planning/phases/01-foundation/01-PATTERNS.md (lines 226–355 — primitive specs)
    - src/styles/tokens.css (consumed variables: `--accent`, `--line`, `--muted`, `--font-serif`)
  </read_first>
  <action>
    Create `src/components/primitives/PulseDot.tsx` and `PulseDot.module.css`. TSX exports a named `PulseDot` function (and default-exports it if convention allows; pick named-export for consistency with PATTERNS barrel) returning `<span className={styles.dot} aria-hidden="true" />`. No props. CSS contains the `.dot` rule lifted from sketch lines 97–101: `width: 8px; height: 8px; border-radius: 50%; background: var(--accent); display: inline-block; animation: pulse 2s infinite;` plus the `@keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.4; } }` keyframe.

    Create `src/components/primitives/UnderlineAccent.tsx` and `UnderlineAccent.module.css`. TSX exports `UnderlineAccent({ children }: { children: React.ReactNode })` returning `<span className={styles.underline}>{children}</span>`. CSS contains the `.underline` rule lifted from sketch lines 110–115: `text-decoration: underline; text-decoration-thickness: 3px; text-underline-offset: 8px; text-decoration-color: var(--accent);`.

    Create `src/components/primitives/TagChip.tsx` and `TagChip.module.css`. TSX exports `TagChip({ children }: { children: React.ReactNode })` returning `<span className={styles.tag}>{children}</span>`. CSS contains the `.tag` rule lifted from sketch lines 206–210: `padding: 5px 12px; border: 1px solid var(--line); border-radius: 100px; font-size: 11px; text-transform: uppercase; letter-spacing: 0.08em; color: var(--muted); display: inline-block;` (add `display: inline-block` because the sketch wraps tags in a flex container; the primitive must work standalone too).

    Create `src/components/primitives/Italic.tsx` and `Italic.module.css`. TSX exports `Italic({ children }: { children: React.ReactNode })` returning `<em className={styles.em}>{children}</em>`. Per PATTERNS.md recommendation: do NOT bake size/color into the primitive — let parent CSS govern via descendant selectors. The Italic primitive's own CSS contains a minimal default for standalone body-text use (matching sketch `.work-desc em` line 205 and `.about-text em` line 252): `.em { font-family: var(--font-serif); font-style: italic; }`. When `Italic` is nested inside a `DisplayHeading`, the `DisplayHeading` module's `.display em { color: var(--accent); }` rule paints it accent. When nested in `Eyebrow` italic variant, the eyebrow's color cascades.

    All four files are server components. No `"use client"`. Each TSX file uses relative-path import for its CSS module (`import styles from "./PulseDot.module.css"` etc.).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const pairs=[['PulseDot','animation: pulse 2s infinite','width: 8px','@keyframes pulse','opacity: 0.4'],['UnderlineAccent','text-decoration-thickness: 3px','text-underline-offset: 8px','var(--accent)'],['TagChip','border: 1px solid var(--line)','border-radius: 100px','text-transform: uppercase','color: var(--muted)','letter-spacing: 0.08em'],['Italic','var(--font-serif)','font-style: italic']];for(const [name,...needs] of pairs){const t=fs.readFileSync('src/components/primitives/'+name+'.tsx','utf8');const c=fs.readFileSync('src/components/primitives/'+name+'.module.css','utf8');if(t.includes('\"use client\"')){console.error(name,'has use client');process.exit(1)}if(!t.includes('export'))process.exit(1);for(const n of needs)if(!c.includes(n)){console.error(name,'css missing',n);process.exit(1)}}"</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/components/primitives/PulseDot.tsx` exports `PulseDot` and renders `<span aria-hidden=` markup
    - source: `src/components/primitives/PulseDot.module.css` contains `width: 8px`, `height: 8px`, `border-radius: 50%`, `background: var(--accent)`, `animation: pulse 2s infinite`, and a `@keyframes pulse` block with `opacity: 0.4` at the 50% step
    - source: `src/components/primitives/UnderlineAccent.tsx` exports `UnderlineAccent` and renders `<span>`
    - source: `src/components/primitives/UnderlineAccent.module.css` contains `text-decoration-thickness: 3px`, `text-underline-offset: 8px`, `text-decoration-color: var(--accent)`
    - source: `src/components/primitives/TagChip.tsx` exports `TagChip` and renders `<span>`
    - source: `src/components/primitives/TagChip.module.css` contains `border: 1px solid var(--line)`, `border-radius: 100px`, `font-size: 11px`, `text-transform: uppercase`, `letter-spacing: 0.08em`, `color: var(--muted)`
    - source: `src/components/primitives/Italic.tsx` exports `Italic` and renders `<em>`
    - source: `src/components/primitives/Italic.module.css` contains `font-family: var(--font-serif)` and `font-style: italic`
    - source: none of the four TSX files contain `"use client"`
  </acceptance_criteria>
  <done>
    Four primitives exist with sketch-faithful visuals. They are server components ready to be imported from anywhere in `src/app/` or `src/components/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Eyebrow and DisplayHeading (composite primitives)</name>
  <read_first>
    - .planning/sketches/006-full-home/index.html (lines 93–96 sans eyebrow, 233–236 and 293–296 italic eyebrow, 103–109 display base, 193–199 work-title, 243–248 about-title, 300–305 contact-title)
    - .planning/phases/01-foundation/01-PATTERNS.md (lines 240–291 — Eyebrow and DisplayHeading specs and prop shapes)
    - src/components/primitives/PulseDot.tsx (Eyebrow `withPulseDot` composes this)
  </read_first>
  <action>
    Create `src/components/primitives/Eyebrow.tsx` and `Eyebrow.module.css`. TSX exports `Eyebrow({ variant = "sans", withPulseDot = false, children }: { variant?: "sans" | "italic"; withPulseDot?: boolean; children: React.ReactNode })`. It returns `<div className={variant === "italic" ? styles.italic : styles.sans}>{withPulseDot ? <PulseDot /> : null}{children}</div>` with `PulseDot` imported relatively from `./PulseDot`. Server component, no `"use client"`.

    CSS contains two classes:
    - `.sans` lifted from sketch lines 93–96: `display: flex; align-items: center; gap: 12px; font-size: 13px; color: var(--muted);` plus add `font-family: var(--font-sans); text-transform: uppercase; letter-spacing: 0.08em;` to match the sketch's `.hero-eyebrow` rendering (the sketch sets these on `.hero-eyebrow span` line 96 area — verify against the file).
    - `.italic` lifted from sketch lines 233–236 / 293–296: `font-family: var(--font-serif); font-style: italic; color: var(--accent); font-size: 22px; display: flex; align-items: center; gap: 12px;` (the flex + gap matches the sans variant so the optional pulse dot lays out the same way).

    Create `src/components/primitives/DisplayHeading.tsx` and `DisplayHeading.module.css`. TSX exports `DisplayHeading({ as = "h1", size = "hero", children }: { as?: "h1" | "h2" | "h3"; size?: "hero" | "work" | "about" | "contact"; children: React.ReactNode })`. Implementation: use a small map `const Tag = as` and return `<Tag className={`${styles.display} ${styles[`size_${size}`]}`}>{children}</Tag>`. Server component.

    CSS contains the base `.display` block lifted from sketch lines 103–109: `font-family: var(--font-serif); font-weight: 400; letter-spacing: -0.04em; line-height: 0.92;`. Plus a descendant selector for `<em>`: `.display em { font-style: italic; color: var(--accent); }` (lines 109 of sketch). Plus four size classes lifted verbatim from sketch:
    - `.size_hero { font-size: clamp(72px, 10vw, 168px); }` (sketch line 104 area)
    - `.size_work { font-size: clamp(48px, 6vw, 80px); line-height: 1; }` (sketch line 193–199)
    - `.size_about { font-size: clamp(48px, 6vw, 84px); line-height: 1.04; letter-spacing: -0.025em; }` (sketch line 243–248)
    - `.size_contact { font-size: clamp(56px, 8vw, 120px); line-height: 1; letter-spacing: -0.035em; }` (sketch line 300–305)

    Note: CSS Modules in Next reject hyphens-in-camelCase issues; use underscores (`size_hero`) so `styles.size_hero` resolves cleanly. The class names in CSS use the same `size_hero` form.

    Updates to `src/components/primitives/index.ts` happen in Task 3 below.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const e=fs.readFileSync('src/components/primitives/Eyebrow.tsx','utf8');const ec=fs.readFileSync('src/components/primitives/Eyebrow.module.css','utf8');const d=fs.readFileSync('src/components/primitives/DisplayHeading.tsx','utf8');const dc=fs.readFileSync('src/components/primitives/DisplayHeading.module.css','utf8');if(!e.includes('variant')||!e.includes('withPulseDot')||!e.includes('PulseDot'))process.exit(1);if(e.includes('\"use client\"'))process.exit(1);if(!ec.includes('.sans')||!ec.includes('.italic')||!ec.includes('var(--muted)')||!ec.includes('var(--accent)')||!ec.includes('var(--font-serif)')||!ec.includes('font-size: 13px')||!ec.includes('font-size: 22px'))process.exit(1);if(!d.includes('as')||!d.includes('size')||!d.includes('h1')||!d.includes('h2')||!d.includes('h3'))process.exit(1);if(d.includes('\"use client\"'))process.exit(1);for(const k of ['letter-spacing: -0.04em','line-height: 0.92','var(--font-serif)','clamp(72px, 10vw, 168px)','clamp(48px, 6vw, 80px)','clamp(48px, 6vw, 84px)','clamp(56px, 8vw, 120px)','color: var(--accent)']){if(!dc.includes(k)){console.error('dh css missing',k);process.exit(1)}}"</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/components/primitives/Eyebrow.tsx` accepts `variant` and `withPulseDot` props with defaults `"sans"` and `false`
    - source: `src/components/primitives/Eyebrow.tsx` imports `PulseDot` from `./PulseDot`
    - source: `src/components/primitives/Eyebrow.tsx` does NOT contain `"use client"`
    - source: `src/components/primitives/Eyebrow.module.css` contains `.sans` and `.italic` rules with `var(--muted)` (sans), `var(--accent)` (italic), `var(--font-serif)` (italic), `font-size: 13px` (sans), `font-size: 22px` (italic)
    - source: `src/components/primitives/DisplayHeading.tsx` accepts `as` (`"h1" | "h2" | "h3"`) and `size` (`"hero" | "work" | "about" | "contact"`) props
    - source: `src/components/primitives/DisplayHeading.tsx` does NOT contain `"use client"`
    - source: `src/components/primitives/DisplayHeading.module.css` contains `letter-spacing: -0.04em`, `line-height: 0.92`, `var(--font-serif)`, `color: var(--accent)`
    - source: `src/components/primitives/DisplayHeading.module.css` contains all four size rules: `clamp(72px, 10vw, 168px)`, `clamp(48px, 6vw, 80px)`, `clamp(48px, 6vw, 84px)`, `clamp(56px, 8vw, 120px)`
  </acceptance_criteria>
  <done>
    Eyebrow and DisplayHeading exist with full variant coverage. All six primitives are now ready to barrel-export and consume.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create primitives barrel and update components barrel</name>
  <read_first>
    - src/components/primitives/ (six TSX files created in Tasks 1 and 2)
    - src/components/index.ts (currently exports only BrandMark and FloatingDock from PLAN 02 Task 3)
    - .planning/phases/01-foundation/01-PATTERNS.md (lines 346–355 — primitives barrel pattern; lines 654–668 — top-level barrel)
  </read_first>
  <action>
    Create `src/components/primitives/index.ts` that named-re-exports all six primitives from their files:
    `export { Eyebrow } from "./Eyebrow";`
    `export { DisplayHeading } from "./DisplayHeading";`
    `export { Italic } from "./Italic";`
    `export { UnderlineAccent } from "./UnderlineAccent";`
    `export { PulseDot } from "./PulseDot";`
    `export { TagChip } from "./TagChip";`

    Update `src/components/index.ts` (currently a two-line chrome re-export from PLAN 02) to add the primitives re-export as a wildcard at the bottom:
    `export { BrandMark } from "./chrome/BrandMark";`
    `export { FloatingDock } from "./chrome/FloatingDock";`
    `export * from "./primitives";`

    The wildcard is acceptable here because the primitives barrel is hand-curated (six known names). It keeps the top-level barrel terse.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=fs.readFileSync('src/components/primitives/index.ts','utf8');for(const n of ['Eyebrow','DisplayHeading','Italic','UnderlineAccent','PulseDot','TagChip'])if(!p.includes(`export { ${n} }`)){console.error('primitives barrel missing',n);process.exit(1)}const i=fs.readFileSync('src/components/index.ts','utf8');if(!i.includes('BrandMark')||!i.includes('FloatingDock')||!i.includes('./primitives'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/components/primitives/index.ts` contains named exports for all six: `Eyebrow`, `DisplayHeading`, `Italic`, `UnderlineAccent`, `PulseDot`, `TagChip`
    - source: `src/components/index.ts` re-exports `BrandMark`, `FloatingDock`, and `* from "./primitives"`
  </acceptance_criteria>
  <done>
    Importing from `@/components/primitives` or `@/components` resolves all six primitives plus the two chrome components. The styleguide page in Task 4 can import them via the barrel.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire primitives into /styleguide page sections</name>
  <read_first>
    - src/app/styleguide/page.tsx (stub anchor sections from PLAN 02 Task 4)
    - src/app/styleguide/page.module.css (section padding + dark-section background from PLAN 02 Task 4)
    - .planning/phases/01-foundation/01-PATTERNS.md (lines 539–579 — styleguide skeleton)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-18 — every primitive variant demonstrated)
    - .planning/sketches/006-full-home/index.html (sections .hero lines 89–166, .work lines 167–230, .about lines 232–272, .contact lines 290–334 — for representative content the styleguide can mimic)
  </read_first>
  <action>
    Rewrite `src/app/styleguide/page.tsx` (it still renders the placeholder text from PLAN 02 Task 4). Keep the four sections in document order with the same ids (`hero`, `work`, `about`, `contact`) and keep `data-darken` on `#about` and `#contact`. Inside each section, demonstrate every primitive variant relevant to that section. The page remains a server component (no `"use client"`).

    Import primitives from `@/components/primitives` (NOT from `@/components` — use the barrel directly per `<interfaces>` so the dependency is explicit).

    Section content (per D-18 — living documentation, not real copy):

    `#hero` section:
    - `<Eyebrow variant="sans" withPulseDot>Product designer · Available May 2026</Eyebrow>` — demonstrates the sans variant with `PulseDot` composition.
    - `<DisplayHeading as="h1" size="hero">I design <Italic>products</Italic> that <UnderlineAccent>feel right</UnderlineAccent> — not just look it.</DisplayHeading>` — demonstrates `size="hero"`, `Italic` nested inside a heading (accent color cascade), and `UnderlineAccent` inline.
    - A `<p>` of body-context italic via `<Italic>` so the standalone body styling is visible.

    `#work` section:
    - `<Eyebrow variant="sans">Selected work · 2024–2026</Eyebrow>`
    - `<DisplayHeading as="h2" size="work">Selected <Italic>work</Italic></DisplayHeading>` — demonstrates `size="work"`.
    - A row of three `<TagChip>` elements: `<TagChip>EDTECH</TagChip><TagChip>STRATEGY</TagChip><TagChip>UI/UX</TagChip>` — demonstrates the chip primitive.

    `#about` section (dark via `data-darken`):
    - `<Eyebrow variant="italic">About me —</Eyebrow>` — demonstrates the italic eyebrow variant.
    - `<DisplayHeading as="h2" size="about">Five years of <Italic>shipping</Italic>.</DisplayHeading>` — demonstrates `size="about"`.

    `#contact` section (dark via `data-darken`):
    - `<Eyebrow variant="italic">Get in touch —</Eyebrow>`
    - `<DisplayHeading as="h2" size="contact">Let's <Italic>talk</Italic>.</DisplayHeading>` — demonstrates `size="contact"`.

    Update `src/app/styleguide/page.module.css` if needed so primitive variants display correctly inside the dark sections. The dark-section rule from PLAN 02 (`section[data-darken] { background: var(--ink); color: var(--bg); }`) cascades down, so the italic eyebrow (which uses `var(--accent)`) and the `DisplayHeading em` (which uses `var(--accent)`) remain orange — that's the intended visual on dark sections too (matches sketch `.about-head` and `.contact-eyebrow`). No CSS module additions strictly required; verify by visual check in Task 5 of PLAN 04.

    Do NOT remove the `min-height: 100vh` from `.section` — PLAN 04 relies on tall sections for the IntersectionObserver / dock test.

    Coverage matrix to verify in the rendered HTML:
    - `Eyebrow variant="sans"`: present in `#hero` (with `withPulseDot`) and `#work` (without).
    - `Eyebrow variant="italic"`: present in `#about` and `#contact`.
    - `DisplayHeading size="hero"|"work"|"about"|"contact"`: one each, in the matching section.
    - `Italic`: nested inside three of the four headings; standalone inside `#hero`'s body paragraph.
    - `UnderlineAccent`: inside the `#hero` headline.
    - `PulseDot`: composed via `Eyebrow withPulseDot` in `#hero`.
    - `TagChip`: three instances inside `#work`.
  </action>
  <verify>
    <automated>node -e "const p=require('fs').readFileSync('src/app/styleguide/page.tsx','utf8');if(p.includes('\"use client\"'))process.exit(1);if(!p.includes('from \"@/components/primitives\"'))process.exit(1);for(const k of ['Eyebrow','DisplayHeading','Italic','UnderlineAccent','PulseDot','TagChip','variant=\"sans\"','variant=\"italic\"','withPulseDot','size=\"hero\"','size=\"work\"','size=\"about\"','size=\"contact\"','id=\"hero\"','id=\"work\"','id=\"about\"','id=\"contact\"','data-darken','EDTECH','STRATEGY']){if(!p.includes(k)){console.error('styleguide missing',k);process.exit(1)}}"</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/app/styleguide/page.tsx` imports from `@/components/primitives`
    - source: `src/app/styleguide/page.tsx` does NOT contain `"use client"`
    - source: `src/app/styleguide/page.tsx` references all six primitive names: `Eyebrow`, `DisplayHeading`, `Italic`, `UnderlineAccent`, `PulseDot` (via `Eyebrow withPulseDot`), `TagChip`
    - source: `src/app/styleguide/page.tsx` uses `Eyebrow` with both `variant="sans"` and `variant="italic"`
    - source: `src/app/styleguide/page.tsx` uses `DisplayHeading` with all four sizes (`hero`, `work`, `about`, `contact`)
    - source: `src/app/styleguide/page.tsx` uses `Eyebrow withPulseDot` at least once
    - source: `src/app/styleguide/page.tsx` includes at least three `<TagChip>` elements with `EDTECH` / `STRATEGY` / `UI/UX`
    - source: `src/app/styleguide/page.tsx` retains `id="hero"`, `id="work"`, `id="about"`, `id="contact"` and `data-darken` on the about/contact sections
    - command: `npm run build` exits 0
    - command: `npm run lint` exits 0
    - behavior: starting `npm run dev` and fetching `http://localhost:3000/styleguide` returns HTML containing the literal text `Available May 2026`, `Selected`, `EDTECH`, `About me —`, `Get in touch —`, and italic-accent renderings of the words `products`, `work`, `shipping`, `talk`
  </acceptance_criteria>
  <done>
    `/styleguide` now demonstrates every primitive variant against both light and dark backgrounds. DS-04 is satisfied: the six shared primitives exist as React components rendering the same visuals as the sketches.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Visual verification of all primitive variants</name>
  <what-built>
    Six primitives wired into `/styleguide` covering every variant: Eyebrow sans (with pulse) + italic; DisplayHeading hero/work/about/contact; Italic inside headings (accent) and in body (no accent); UnderlineAccent in hero; PulseDot via Eyebrow composition; TagChip row in work.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit `http://localhost:3000/styleguide`.
    2. In `#hero`: pulse dot pulses at 2s loop, sans uppercase eyebrow renders next to it in Inter 13px muted, hero headline renders in Instrument Serif at the largest size with `products` italic-orange, `feel right` underlined with a 3px orange line 8px below the text.
    3. In `#work`: sans eyebrow, work headline with italic-orange `work`, three pill-shaped chips with uppercase tracked labels.
    4. In `#about` (dark): italic Instrument Serif eyebrow in orange, about headline with italic-orange `shipping`.
    5. In `#contact` (dark): italic Instrument Serif eyebrow, contact headline with italic-orange `talk` at the largest contact size.
    6. Resize the window from 1920px down to 800px and observe `clamp()` sizes scale smoothly without jumping.
    7. Confirm no TypeScript or lint errors in the dev server log.
  </how-to-verify>
  <resume-signal>Reply `approved` if all seven points pass. If any primitive looks wrong against the sketch (color off, weight wrong, animation broken), describe the failure and which primitive/task to revisit.</resume-signal>
</task>

</tasks>

<verification>
After all five tasks: `npm run build` and `npm run lint` exit 0; `/styleguide` renders all six primitives in all documented variants; visual verification confirms sketch-matching output.
</verification>

<success_criteria>
- DS-04 satisfied: All six primitives (`Eyebrow`, `DisplayHeading`, `Italic`, `UnderlineAccent`, `PulseDot`, `TagChip`) exist and render with sketch-matching visuals.
- DS-03 fully satisfied: Spacing scale and radius scale exist via the literal pixel values consumed inside primitive CSS Modules (no `--space-*` tokens needed, per the planner decision in PLAN 02). Roadmap SC #3 ("Primitive components render with the same visual output as the sketches") verified by Task 5.
- `/styleguide` is now living documentation, reachable at `/styleguide` and demonstrating every primitive variant.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` summarizing: six primitives built, barrel created, `/styleguide` wired, visual verification result, any deviations from sketch line ranges.
</output>
